/* Este arquivo/file, permite utilizar redireccionamentos fora dos componentes React - noutros arquivos como o "requests", por exemplo  */

import { createBrowserHistory } from "history";

export default createBrowserHistory();